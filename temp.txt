   var typesel;
        var type = content.charBeforeToken();

        // wrap of previous
        if ('>+'.indexOf(token) != -1) {
            // if not started with >+
            if (token != content.currentChar()) {
                if (type.length == 0) {
                    typesel = new _TypeSelector('*');
                } else {
                    typesel = new _TypeSelector(type);
                }
                content.nextToken();
                this.parser.addSelector(typesel);
                this.parser.nextTypeParser();
                return;
            }

            typesel = this.parser.lastSelector();
            if (!typesel) {
                throw 'previous selector before token:' + token + ' not found.'
            }

            content.skip(1);
            this.parser.addSelector(typesel);
            this.parser.nextTypeParser();
            return;
        }

        console.log('charBeforeToken', type);
        if (type.length == 0) {
            typesel = new _TypeSelector('*');
        } else {
            typesel = new _TypeSelector(type);
        }
